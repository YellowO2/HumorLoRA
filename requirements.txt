# Minimal requirements to run eval and training scripts.
# Install with: pip install -r requirements.txt
# Also requires: ollama (https://ollama.com) installed separately as a system service.

torch
unsloth
transformers
peft
trl
accelerate
datasets
pandas
numpy
requests
scipy
scikit-learn
